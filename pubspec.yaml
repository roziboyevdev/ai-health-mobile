name: ai_health_mobile
description: "Offline-first Flutter app for Veepoo / HBand smart bands."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.1
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1
  go_router: ^16.2.5
  hooks_riverpod: ^3.0.3
  intl: ^0.20.2
  path_provider: ^2.1.5
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
